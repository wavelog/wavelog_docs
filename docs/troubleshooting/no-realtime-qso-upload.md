# No Realtime QSO Upload When Logging via API

Realtime upload will only happen when you use the logging interface in Wavelog itself. If you run any third-party programs and upload via the API, no realtime upload will happen.